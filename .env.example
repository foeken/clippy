# Clippy Configuration
# Copy this file to .env and customize for your environment.

# Cloud environment: "commercial" (default) or "gcc" (Office 365 US Government)
CLIPPY_CLOUD=commercial

# Optional: skip browser login by providing a token directly
# CLIPPY_TOKEN=

# Optional: enable read-only mode (blocks write actions like send/respond/create/delete)
# CLIPPY_READONLY=false
